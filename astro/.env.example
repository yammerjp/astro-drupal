# Astro Environment Variables Example

# Drupal API endpoint (internal Docker network URL)
DRUPAL_BASE_URL=http://drupal:8080

# Allowed hosts for Vite development server
# Add your hostname here (e.g., "localhost,your-hostname.local,192.168.1.100")
ALLOWED_HOSTS=localhost

# Copy this file to .env and customize for your environment:
# cp .env.example .env