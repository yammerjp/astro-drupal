FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci

# Copy application files
COPY . .

# Expose port
EXPOSE 4321

# Development command
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]